/**
 * =========================================================
 * TYPES BARREL (ESPN CORE ENTRY POINT)
 * =========================================================
 * Single source of truth exports for the entire frontend.
 * Keeps imports clean: import from "@/types"
 * =========================================================
 */

export type { CanonicalPost, MediaImage } from "./content";

/**
 * OPTIONAL (future-safe expansion point)
 * Add shared UI or API types here later
 *
 * Example:
 * export type { ApiResponse } from "./api";
 * export type { HomepageFeed } from "@/lib/orchestrator/homepage";
 */